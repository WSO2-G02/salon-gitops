apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: user_service
  namespace: staging
spec:
  hosts:
  - "*"
  gateways:
  - salon-gateway
  http:
  - route:
    - destination:
        host: user_service
        port:
          number: 80
