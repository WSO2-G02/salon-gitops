apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: user-service
  namespace: staging
spec:
  hosts:
  - "*"
  gateways:
  - istio-system/salon-gateway
  http:
  - match:
    - uri:
        prefix: /user-service
    route:
    - destination:
        host: user-service
        port:
          number: 80
