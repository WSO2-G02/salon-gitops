apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: reports_analytics
  namespace: staging
spec:
  hosts:
  - "api.salon.example.com"
  gateways:
  - salon-gateway
  http:
  - route:
    - destination:
        host: reports_analytics
        port:
          number: 80
