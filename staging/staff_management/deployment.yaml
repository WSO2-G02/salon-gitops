apiVersion: apps/v1
kind: Deployment
metadata:
  name: staff-management
  namespace: staging
spec:
  replicas: 1
  # Rolling Update: Zero-downtime deployment with automatic rollback protection
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: staff-management
  template:
    metadata:
      labels:
        app: staff-management
    spec:
      imagePullSecrets:
      - name: ecr-registry-secret
      containers:
      - name: staff-management
        image: 024955634588.dkr.ecr.eu-north-1.amazonaws.com/staff_management:0ebfe38f-20251213161310
        ports:
        - containerPort: 8003
        # Readiness: Remove from service if unhealthy
        readinessProbe:
          httpGet:
            path: /health
            port: 8003
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
          successThreshold: 1
        # Liveness: Restart pod if unresponsive
        livenessProbe:
          httpGet:
            path: /health
            port: 8003
          initialDelaySeconds: 15
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 3
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        env:
        - name: JWT_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: JWT_SECRET_KEY
        - name: DB_HOST
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: DB_HOST
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: DB_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: DB_PASSWORD
      imagePullSecrets:
        - name: aws-ecr-cred
