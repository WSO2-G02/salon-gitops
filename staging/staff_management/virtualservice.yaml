apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: staff_management
  namespace: staging
spec:
  hosts:
  - "*"
  gateways:
  - salon-gateway
  http:
  - route:
    - destination:
        host: staff_management
        port:
          number: 80
